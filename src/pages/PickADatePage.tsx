import React from 'react';
import { PickADate } from '../component/PickADatePage';

interface Props {}

export const PickADatePage = (props: Props) => {
  return (
    <>
      <PickADate />
    </>
  );
};
