import React from 'react';
import { CustomCake } from '../components/CustomCakePage';

interface Props {}

export const CustomCakePage = (props: Props) => {
  return (
    <>
      <CustomCake />
    </>
  );
};
