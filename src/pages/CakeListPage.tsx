import React from 'react';
import { CakesList } from '../component/CakesListPage/CakesList';

interface Props {}

export const CakesListPage = (props: Props) => {
  return (
    <div>
      <CakesList />
    </div>
  );
};
