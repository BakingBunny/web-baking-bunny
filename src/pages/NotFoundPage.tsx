import React from 'react';
import { NotFound } from '../component/NotFound';

interface Props {}

export const NotFoundPage = (props: Props) => {
  return (
    <div>
      <NotFound />
    </div>
  );
};
