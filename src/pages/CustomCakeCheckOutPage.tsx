import React from 'react';
import { CustomCakeCheckOut } from '../components/CustomCakeCheckOutPage';

interface Props {}

export const CustomCakeCheckOutPage = (props: Props) => {
  window.scrollTo(0, 0);

  return <CustomCakeCheckOut />;
};
