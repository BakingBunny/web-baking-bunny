import React from 'react';
import { Cakes } from '../component/CakePage/Cakes';

interface Props {}

export const CakePage = (props: Props) => {
  return (
    <div>
      <Cakes />
    </div>
  );
};
